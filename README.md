# 📂 Python Concurrency Benchmark: File Character Counter

โปรเจกต์นี้จัดทำขึ้นเพื่อศึกษาและเปรียบเทียบประสิทธิภาพการเขียนโปรแกรมแบบ Concurrency 3 รูปแบบในภาษา Python ได้แก่ **Threading**, **Asyncio**, และ **Multiprocessing** โดยใช้โจทย์การอ่านไฟล์และนับจำนวนตัวอักษร (File I/O & Text Processing)

## 🎯 วัตถุประสงค์ (Objective)
1. เพื่อทำความเข้าใจความแตกต่างระหว่างการทำงานแบบ **Concurrency** (สลับกันทำ) และ **Parallelism** (ทำพร้อมกันจริง)
2. เพื่อเปรียบเทียบประสิทธิภาพ (Benchmark) เมื่อ Python ต้องจัดการงาน 2 ประเภท:
   - **I/O Bound:** การรออ่านไฟล์จาก Disk
   - **CPU Bound:** การประมวลผลนับตัวอักษรและตัดช่องว่าง

## 🛠️ โครงสร้างและการทำงาน (Architecture)

โปรแกรมถูกแบ่งออกเป็น 3 โมดูลหลัก ตามรูปแบบการทำงาน:

### 1. Threading (`thread_version.py`)
- **Library:** `concurrent.futures.ThreadPoolExecutor`
- **การทำงาน:** สร้าง Thread แยกเพื่อไปอ่านไฟล์พร้อมๆ กัน
- **จุดเด่น:** ระบบปฏิบัติการ (OS) เป็นผู้จัดการ Thread สามารถสลับการทำงานได้ทันทีเมื่อ Thread หนึ่งติดสถานะรอ (Waiting for I/O) ทำให้การอ่านไฟล์หลายๆ ไฟล์เสร็จเร็วกว่าการทำทีละไฟล์มาก

### 2. Asyncio (`asyncio_version.py`)
- **Library:** `asyncio`
- **การทำงาน:** ใช้ Event Loop ในการจัดการ Task แบบ Single-threaded
- **เทคนิคพิเศษ:** เนื่องจาก Python ไม่มี Async File I/O แบบ Native จึงใช้ `loop.run_in_executor` เพื่อผลักภาระงานอ่านไฟล์ (Blocking I/O) ไปทำใน Thread Pool เบื้องหลัง ทำให้ Main Loop ไม่หยุดชะงัก

### 3. Process Pool (`process_version.py`)
- **Library:** `concurrent.futures.ProcessPoolExecutor`
- **การทำงาน:** สร้าง Process ใหม่แยกออกจากกัน (Separate Memory Space) โดยแต่ละ Process มี Python Interpreter เป็นของตัวเอง
- **จุดเด่น:** เป็นวิธีเดียวใน Python ที่สามารถใช้งาน CPU ได้หลาย Core พร้อมกัน (Bypass GIL)

---

## 🚀 วิธีการใช้งาน (Usage)

1. **เตรียมข้อมูล:** สร้างโฟลเดอร์ `data/` และนำไฟล์ `.txt` ที่ต้องการทดสอบไปวางไว้
2. **รันโปรแกรม:** เปิด Terminal แล้วพิมพ์คำสั่ง:
   ```bash
   python main.py